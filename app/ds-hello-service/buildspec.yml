version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21

  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $ECR_URI
      - IMAGE_TAG=$CODEBUILD_RESOLVED_SOURCE_VERSION
      - APP_DIR=$CODEBUILD_SRC_DIR/app/ds-hello-service

  build:
    commands:
      - echo Running tests...
      - cd $CODEBUILD_SRC_DIR/app/ds-hello-service && ./gradlew test --no-daemon
      - echo Building Docker image...
      - cd $CODEBUILD_SRC_DIR/app/ds-hello-service && docker build -t $ECR_URI:$IMAGE_TAG .
      - docker tag $ECR_URI:$IMAGE_TAG $ECR_URI:latest

  post_build:
    commands:
      - docker push $ECR_URI:$IMAGE_TAG
      - docker push $ECR_URI:latest
      - cd $CODEBUILD_SRC_DIR/app/ds-hello-service && printf '[{"name":"%s","imageUri":"%s"}]' $CONTAINER_NAME $ECR_URI:$IMAGE_TAG > imagedefinitions.json
      - echo Build complete

reports:
  test-reports:
    files:
      - "app/ds-hello-service/build/test-results/test/*.xml"
    file-format: JUNITXML

artifacts:
  files:
    - app/ds-hello-service/imagedefinitions.json
